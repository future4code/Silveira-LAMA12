import UserModel from "../Model/UserModel";
import { userType } from "../Types/UserTypes";



export default class UserBusiness {
    public async Signup(user: userType) {
        const { name, email, password } = user


    }
}